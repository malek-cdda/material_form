export interface headCellsProps {
  id: string;
  numeric: boolean;
  disablePadding: boolean;
  label: string;
}
export interface rowDataProps {
  img: string;
  id: number;
  name: string;
  address?: string;
  status: string;
  join: string;
  agent: string;
  buyer: string;
}
